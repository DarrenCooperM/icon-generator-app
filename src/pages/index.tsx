import { type NextPage } from "next";
import { useSession } from "next-auth/react";
import Head from "next/head";
import ImageSlider from "~/component/ImageSlider";
import { PrimaryLinkButton } from "~/component/PrimaryLinkButton";

function HeroBanner() {
  const { data: sessionData } = useSession();

  const images = [
    "/horse.jpg",
    "/horse1.jpg",
    "/horse2.jpg",
    "/horse3.jpg",
    "/horse4.jpg",
    "/horse5.jpg",
  ];

  return (
    <>
      <section className="mb-24 mt-12 grid items-center px-8 text-center sm:mt-24 xl:grid xl:grid-cols-2 xl:items-start xl:gap-12 xl:text-left">
        <div className="flex flex-col gap-4">
          <h1 className="text-2xl font-bold ">
            {sessionData && <span>Kia Ora {sessionData.user?.name}!</span>}
          </h1>
          <h1 className="text-6xl font-bold ">
            Generate Logos for your business at the click of a button
          </h1>
          <p className="mt-8 text-2xl">
            Use AI to generate logos in seconds instead of paying a designer and
            waiting for them to create them for you.
          </p>
          <PrimaryLinkButton
            href="/generate"
            className="xl:grid-col-1 mx-24 mb-10 mt-12 grid xl:mx-0 xl:grid xl:self-start"
          >
            Generated your logo now!
          </PrimaryLinkButton>
        </div>
        <div className="ml-4 grid items-center md:m-0 xl:ml-0">
          <ImageSlider images={images} />
        </div>
      </section>
    </>
  );
}

const HomePage: NextPage = () => {
  return (
    <>
      <Head>
        <title>Logo Generator from MaoriWebDev</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/maori.jpg" />
      </Head>
      <main className="container mx-auto flex flex-col items-center justify-center">
        <HeroBanner />
      </main>
    </>
  );
};

export default HomePage;
